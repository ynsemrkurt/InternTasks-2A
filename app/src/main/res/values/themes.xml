<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.InternTasks2A" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Typography styles -->
        <item name="textAppearanceTitleLarge">@style/LargeTextStyle</item>
        <item name="textAppearanceTitleMedium">@style/MediumTextStyle</item>
        <item name="textAppearanceTitleSmall">@style/SmallTextStyle</item>
        <item name="textAppearanceBody1">@style/XSmallTextStyle</item>
        <item name="textAppearanceBody2">@style/XSmallBoldTextStyle</item>

        <!-- Button styles -->
        <item name="buttonStyle">@style/CustomButtonStyle</item>

        <!-- Color styles -->
        <item name="strokeColor">@color/day_color</item>
        <item name="android:textColor">@color/day_color</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>